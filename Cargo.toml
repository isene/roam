[package]
name = "roam"
version = "0.1.4"
edition = "2021"
description = "Wi-Fi and VPN in the terminal, straight to NetworkManager, and nothing running while you are not looking"
license = "Unlicense"
repository = "https://github.com/isene/roam"
authors = ["Geir Isene <g@isene.com>"]

[dependencies]
crust = { version = "0.1", path = "../crust", package = "fe2o3-crust" }
zbus = { version = "5", features = ["blocking-api"] }
# 1.7 keeps an idle helper thread forever, waking it twice a second.
blocking = "=1.6.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
